import { inArray } from '../../../helpers/in-array';
import { module, test } from 'qunit';

module('Unit | Helper | in array');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = inArray(42);
  assert.ok(result);
});
